# trex-init writes generated dev secrets (root.env, derived.env) here on first
# boot. Track this dir (so it exists pre-owned and Docker doesn't recreate it
# root-owned) but never commit the generated secrets.
*
!.gitignore
