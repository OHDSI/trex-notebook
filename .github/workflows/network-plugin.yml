name: network-plugin

on:
  pull_request:
    paths:
      - 'plugins/network/**'
      - '.github/workflows/network-plugin.yml'
  push:
    branches: [develop, main]
    paths:
      - 'plugins/network/**'

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@ohdsi'
      - name: Install
        working-directory: plugins/network
        run: npm install
        env:
          # @ohdsi/atlas-ui is served from GitHub Packages, which requires auth
          # even for reads; without a token npm install 401s.
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Typecheck
        working-directory: plugins/network
        run: npm run typecheck
      - name: Unit test
        working-directory: plugins/network
        run: npx vitest run
      - name: Build
        working-directory: plugins/network
        run: npm run build
      - name: Verify bundle
        run: test -f plugins/sibyl/public/plugins/network-plugin/index.system.js
